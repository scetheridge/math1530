A jump page template for a math course.  Populate the JSON object in the javascript file and the buttons populate the page using AngularJS. 
Units are auto added to the menu at the top. Specify the font awesome icon to use.  
The format is 
Units - unit title, array of section objects
Sections - section title, array of link objects
Link - link text, link url, link icon, optional link description


 